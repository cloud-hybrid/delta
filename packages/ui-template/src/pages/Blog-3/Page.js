import PropTypes from "prop-types";

import React from "react";

export const Page = ({}) => {
    return (
        <div>
            hello world
        </div>
    );
};

Page.defaultProps = {};

Page.propTypes = {};

export default Page;